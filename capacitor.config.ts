import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vamsi.marathonprep',
  appName: 'Runtoday',
  webDir: '.output/public'
};

export default config;
